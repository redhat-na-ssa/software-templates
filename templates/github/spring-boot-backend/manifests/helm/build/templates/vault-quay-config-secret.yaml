kind: Secret
apiVersion: v1
metadata:
  name: {{ .Values.app.name }}-registry-creds
  namespace: {{ .Values.app.name }}-dev
data:
  .dockerconfigjson: >-
    ewog_xxxxxx
type: kubernetes.io/dockerconfigjson
