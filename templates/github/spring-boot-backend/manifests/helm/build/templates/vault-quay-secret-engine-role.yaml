apiVersion: redhatcop.redhat.io/v1alpha1
kind: QuaySecretEngineRole
metadata:
  name: {{ .Values.app.name }}
  namespace: vault-admin
spec:
  authentication: 
    path: kubernetes
    role: policy-admin
  path: quay
  namespaceName: {{ .Values.image.organization }}
  createRepositories: true
  defaultPermission: write
  TTL: "8760h" #1 year
  